// Code generated by velox. DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"

	velox "github.com/syssam/velox"
	sqlgraph "github.com/syssam/velox/dialect/sql/sqlgraph"
	runtime "github.com/syssam/velox/runtime"
	field "github.com/syssam/velox/schema/field"
	entity "github.com/test/project/ent/entity"
	post "github.com/test/project/ent/post"
)

// postIDFieldType is the field.Type for Post's primary key column.
var postIDFieldType = field.TypeInt64

// postFieldTypes maps column names to field.Type for Post.
var postFieldTypes = map[string]field.Type{"id": field.TypeInt64, "title": field.TypeString, "content": field.TypeString}

// PostCreate is the builder for creating a Post entity.
type PostCreate struct {
	config   runtime.Config
	mutation *PostMutation
	hooks    []runtime.Hook
}

// NewPostCreate creates a new PostCreate builder.
func NewPostCreate(c runtime.Config, mutation *PostMutation, hooks []runtime.Hook) *PostCreate {
	return &PostCreate{
		config:   c,
		hooks:    hooks,
		mutation: mutation,
	}
}

// SetTitle sets the "title" field.
func (c *PostCreate) SetTitle(v string) *PostCreate {
	c.mutation.SetTitle(v)
	return c
}

// SetContent sets the "content" field.
func (c *PostCreate) SetContent(v string) *PostCreate {
	c.mutation.SetContent(v)
	return c
}

// SetAuthorID sets the "author" edge by id.
func (c *PostCreate) SetAuthorID(id int64) *PostCreate {
	c.mutation.SetAuthorID(id)
	return c
}

// SetNillableAuthorID sets the "author" edge by id if the given value is not nil.
func (c *PostCreate) SetNillableAuthorID(id *int64) *PostCreate {
	if id != nil {
		c.SetAuthorID(*id)
	}
	return c
}

// SetAuthor sets the "author" edge to the given entity.
func (c *PostCreate) SetAuthor(v *entity.User) *PostCreate {
	c.SetAuthorID(v.ID)
	return c
}

// Mutation returns the PostMutation.
func (c *PostCreate) Mutation() *PostMutation {
	return c.mutation
}

// check runs all checks and user-defined validators on the builder.
func (c *PostCreate) check() error {
	if _, ok := c.mutation.Title(); !ok {
		return &runtime.ValidationError{
			Entity: "Post",
			Err:    errors.New("missing required field \"Post.title\""),
			Field:  "title",
			Name:   "title",
		}
	}
	if _, ok := c.mutation.Content(); !ok {
		return &runtime.ValidationError{
			Entity: "Post",
			Err:    errors.New("missing required field \"Post.content\""),
			Field:  "content",
			Name:   "content",
		}
	}
	if len(c.mutation.AuthorIDs()) == 0 {
		return &runtime.ValidationError{
			Entity: "Post",
			Err:    errors.New("missing required edge \"Post.author\""),
			Field:  "author",
			Name:   "author",
		}
	}
	return nil
}

// sqlSave executes the SQL create for Post after hooks have run.
func (c *PostCreate) sqlSave(ctx context.Context) (*entity.Post, error) {
	if err := c.check(); err != nil {
		return nil, err
	}
	_node, _spec := c.createSpec()
	if err := sqlgraph.CreateNode(ctx, c.config.Driver, _spec); err != nil {
		return nil, runtime.MayWrapConstraintError(err)
	}
	_node.SetConfig(c.config)
	if _spec.ID.Value != nil {
		id := _spec.ID.Value.(int64)
		_node.ID = int64(id)
	}
	c.mutation.SetID(_node.ID)
	return _node, nil
}

// createSpec builds the Post node and sqlgraph.CreateSpec from mutation fields.
func (c *PostCreate) createSpec() (*entity.Post, *sqlgraph.CreateSpec) {
	var (
		_node = &entity.Post{}
		_spec = sqlgraph.NewCreateSpec(post.Table, &sqlgraph.FieldSpec{
			Column: post.FieldID,
			Type:   postIDFieldType,
		})
	)
	if c.mutation._title != nil {
		v := *c.mutation._title
		_spec.SetField("title", field.TypeString, v)
		_node.Title = v
	}
	if c.mutation._content != nil {
		v := *c.mutation._content
		_spec.SetField("content", field.TypeString, v)
		_node.Content = v
	}
	if nodes := c.mutation.AuthorIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Bidi:    false,
			Columns: []string{"user_posts"},
			Inverse: true,
			Rel:     sqlgraph.M2O,
			Table:   "posts",
			Target: &sqlgraph.EdgeTarget{IDSpec: &sqlgraph.FieldSpec{
				Column: "id",
				Type:   field.TypeInt64,
			}},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// Save creates the Post in the database.
func (c *PostCreate) Save(ctx context.Context) (*entity.Post, error) {
	hooks := c.hooks
	return velox.WithHooks[*entity.Post, PostMutation, *PostMutation](ctx, c.sqlSave, c.mutation, hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (c *PostCreate) SaveX(ctx context.Context) *entity.Post {
	v, err := c.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (c *PostCreate) Exec(ctx context.Context) error {
	_, err := c.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (c *PostCreate) ExecX(ctx context.Context) {
	if err := c.Exec(ctx); err != nil {
		panic(err)
	}
}

// PostCreateBulk is the builder for creating many Post entities in bulk.
type PostCreateBulk struct {
	config    runtime.Config
	err       error
	builders  []*PostCreate
	batchSize int
}

// NewPostCreateBulk creates a new PostCreateBulk builder.
func NewPostCreateBulk(c runtime.Config, builders []*PostCreate) *PostCreateBulk {
	return &PostCreateBulk{
		builders: builders,
		config:   c,
	}
}

// BatchSize sets the chunk size for the bulk insert.
// A value of 0 (default) means no chunking — all builders
// go through a single BatchCreate. When set, Save splits the
// builders into chunks of at most n and runs one INSERT per
// chunk. Use this to stay under SQLite's SQL-variable cap.
//
// Atomicity across chunks is NOT automatic. Wrap the call
// in client.Tx(ctx) if you need all-or-nothing semantics.
func (_cb *PostCreateBulk) BatchSize(n int) *PostCreateBulk {
	_cb.batchSize = n
	return _cb
}

// Save creates the Post entities in the database.
//
// When BatchSize is 0 (default), all builders go through a
// single mutator chain ending in one BatchCreate. When
// BatchSize > 0, the builders are sliced into chunks of at
// most N and each chunk runs its own mutator chain + one
// INSERT. A mid-loop error returns the rows persisted so far
// together with the error — wrap in client.Tx(ctx) for
// all-or-nothing semantics.
func (_cb *PostCreateBulk) Save(ctx context.Context) ([]*entity.Post, error) {
	if _cb.err != nil {
		return nil, _cb.err
	}
	if _cb.batchSize <= 0 || _cb.batchSize >= len(_cb.builders) {
		return _cb.saveChunk(ctx, _cb.builders)
	}
	nodes := make([]*entity.Post, 0, len(_cb.builders))
	for start := 0; start < len(_cb.builders); start += _cb.batchSize {
		end := start + _cb.batchSize
		if end > len(_cb.builders) {
			end = len(_cb.builders)
		}
		chunk, err := _cb.saveChunk(ctx, _cb.builders[start:end])
		nodes = append(nodes, chunk...)
		if err != nil {
			return nodes, err
		}
	}
	return nodes, nil
}

// saveChunk runs one mutator chain over a sub-slice of the builders.
func (_cb *PostCreateBulk) saveChunk(ctx context.Context, builders []*PostCreate) ([]*entity.Post, error) {
	if len(builders) == 0 {
		return []*entity.Post{}, nil
	}
	specs := make([]*sqlgraph.CreateSpec, len(builders))
	nodes := make([]*entity.Post, len(builders))
	mutators := make([]runtime.Mutator, len(builders))
	for i := range builders {
		func(i int, root context.Context) {
			builder := builders[i]
			var mut runtime.Mutator = runtime.MutateFunc(func(ctx context.Context, m runtime.Mutation) (runtime.Value, error) {
				mutation, ok := m.(*PostMutation)
				if !ok {
					return nil, fmt.Errorf("velox: unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				nodes[i].SetConfig(_cb.config)
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					if err = sqlgraph.BatchCreate(ctx, _cb.config.Driver, spec); err != nil {
						err = runtime.MayWrapConstraintError(err)
					}
				}
				if err != nil {
					return nil, err
				}
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int64(id)
					mutation.SetID(int64(id))
				}
				return nodes[i], nil
			})
			for j := len(builder.hooks) - 1; j >= 0; j-- {
				mut = builder.hooks[j](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (_cb *PostCreateBulk) SaveX(ctx context.Context) []*entity.Post {
	v, err := _cb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (_cb *PostCreateBulk) Exec(ctx context.Context) error {
	_, err := _cb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (_cb *PostCreateBulk) ExecX(ctx context.Context) {
	if err := _cb.Exec(ctx); err != nil {
		panic(err)
	}
}
