// Code generated by velox. DO NOT EDIT.

package ent

import (
	"context"
	"fmt"

	velox "github.com/syssam/velox"
	sql "github.com/syssam/velox/dialect/sql"
	runtime "github.com/syssam/velox/runtime"
	entity "github.com/test/project/ent/entity"
	predicate "github.com/test/project/ent/predicate"
)

// EmployeeMutation represents an operation that mutates the Employee nodes in the graph.
type EmployeeMutation struct {
	config              runtime.Config
	op                  runtime.Op
	id                  *int64
	_name               *string
	clearedFields       map[string]struct{}
	oldValue            func(context.Context) (*entity.Employee, error)
	oldLoaded           bool
	oldCache            *entity.Employee
	subordinates        map[int64]struct{}
	removedSubordinates map[int64]struct{}
	clearedSubordinates bool
	manager             map[int64]struct{}
	removedManager      map[int64]struct{}
	clearedManager      bool
	predicates          []predicate.Employee
}

var _ velox.Mutation = (*EmployeeMutation)(nil)

// employeeOption allows management of the mutation configuration using functional options.
type employeeOption func(*EmployeeMutation)

// NewEmployeeMutation creates new mutation for the Employee entity.
func NewEmployeeMutation(c runtime.Config, op runtime.Op, opts ...employeeOption) *EmployeeMutation {
	m := &EmployeeMutation{
		config: c,
		op:     runtime.Op(op),
	}
	for _, opt := range opts {
		opt(m)
	}
	return m
}

// Op returns the operation name.
func (m *EmployeeMutation) Op() runtime.Op {
	return m.op
}

// SetOp allows setting the mutation operation.
func (m *EmployeeMutation) SetOp(op runtime.Op) {
	m.op = op
}

// Type returns the schema type name of this mutation ("Employee").
func (m *EmployeeMutation) Type() string {
	return "Employee"
}

// ID returns the ID value in the mutation, if it was provided by the caller.
// The second bool return indicates whether the ID field was set.
func (m *EmployeeMutation) ID() (id any, exists bool) {
	if m.id == nil {
		return
	}
	return *m.id, true
}

// SetID sets the entity ID value on the mutation.
func (m *EmployeeMutation) SetID(id int64) {
	m.id = &id
}

// loadOld lazily loads the pre-mutation entity state for OldXxx methods.
func (m *EmployeeMutation) loadOld(ctx context.Context) (*entity.Employee, error) {
	if m.op != runtime.OpUpdateOne {
		return nil, fmt.Errorf("OldField is only allowed on UpdateOne operations")
	}
	if m.oldValue == nil {
		return nil, fmt.Errorf("OldField is not supported: no old value loader configured")
	}
	if m.oldLoaded {
		return m.oldCache, nil
	}
	old, err := m.oldValue(ctx)
	if err != nil {
		return nil, fmt.Errorf("querying old values: %w", err)
	}
	m.oldCache = old
	m.oldLoaded = true
	return old, nil
}

// Where appends a list predicates to the EmployeeMutation builder.
func (m *EmployeeMutation) Where(ps ...predicate.Employee) {
	m.predicates = append(m.predicates, ps...)
}

// PredicatesFuncs returns the predicates as untyped selector functions.
func (m *EmployeeMutation) PredicatesFuncs() []func(*sql.Selector) {
	ps := make([]func(*sql.Selector), len(m.predicates))
	for i, p := range m.predicates {
		ps[i] = p
	}
	return ps
}

// SetName sets the "name" field.
func (m *EmployeeMutation) SetName(v string) {
	m._name = &v
}

// Name returns the value of the "name" field in the mutation.
func (m *EmployeeMutation) Name() (r string, exists bool) {
	if m._name == nil {
		return
	}
	return *m._name, true
}

// ResetName resets all changes to the "name" field.
func (m *EmployeeMutation) ResetName() {
	m._name = nil
	delete(m.clearedFields, "name")
}

// AddSubordinateIDs adds the "subordinates" edge to the Employee entity by ids.
func (m *EmployeeMutation) AddSubordinateIDs(ids ...int64) {
	if m.subordinates == nil {
		m.subordinates = make(map[int64]struct{})
	}
	for _, id := range ids {
		m.subordinates[id] = struct{}{}
	}
}

// RemoveSubordinateIDs removes the "subordinates" edge to the Employee entity by ids.
func (m *EmployeeMutation) RemoveSubordinateIDs(ids ...int64) {
	if m.removedSubordinates == nil {
		m.removedSubordinates = make(map[int64]struct{})
	}
	for _, id := range ids {
		delete(m.subordinates, id)
		m.removedSubordinates[id] = struct{}{}
	}
}

// ClearSubordinates clears the "subordinates" edge.
func (m *EmployeeMutation) ClearSubordinates() {
	m.clearedSubordinates = true
}

// SubordinatesCleared reports if the "subordinates" edge was cleared.
func (m *EmployeeMutation) SubordinatesCleared() bool {
	return m.clearedSubordinates
}

// RemovedSubordinatesIDs returns the removed IDs of the "subordinates" edge.
func (m *EmployeeMutation) RemovedSubordinatesIDs() (ids []int64) {
	for id := range m.removedSubordinates {
		ids = append(ids, id)
	}
	return
}

// ResetSubordinates resets all changes to the "subordinates" edge.
func (m *EmployeeMutation) ResetSubordinates() {
	m.subordinates = nil
	m.clearedSubordinates = false
	m.removedSubordinates = nil
}

// SubordinatesIDs returns the "subordinates" edge IDs in the mutation.
func (m *EmployeeMutation) SubordinatesIDs() (ids []int64) {
	for id := range m.subordinates {
		ids = append(ids, id)
	}
	return
}

// SetManagerID sets the "manager" edge to the Employee entity by id.
func (m *EmployeeMutation) SetManagerID(id int64) {
	if m.manager == nil {
		m.manager = make(map[int64]struct{})
	}
	m.manager[id] = struct{}{}
}

// ClearManager clears the "manager" edge.
func (m *EmployeeMutation) ClearManager() {
	m.clearedManager = true
}

// ManagerCleared reports if the "manager" edge was cleared.
func (m *EmployeeMutation) ManagerCleared() bool {
	return m.clearedManager
}

// ResetManager resets all changes to the "manager" edge.
func (m *EmployeeMutation) ResetManager() {
	m.manager = nil
	m.clearedManager = false
	m.removedManager = nil
}

// ManagerIDs returns the "manager" edge IDs in the mutation.
func (m *EmployeeMutation) ManagerIDs() (ids []int64) {
	for id := range m.manager {
		ids = append(ids, id)
	}
	return
}

// Fields returns all fields that were changed during this mutation.
func (m *EmployeeMutation) Fields() []string {
	var fields []string
	if m._name != nil {
		fields = append(fields, "name")
	}
	return fields
}

// Field returns the value of a field with the given name.
func (m *EmployeeMutation) Field(name string) (runtime.Value, bool) {
	switch name {
	case "name":
		if m._name != nil {
			return *m._name, true
		}
	}
	return nil, false
}

// SetField sets the value of a field with the given name. It returns an error if the field is not defined in the schema, or if the value type does not match.
func (m *EmployeeMutation) SetField(name string, value runtime.Value) error {
	switch name {
	case "name":
		v, ok := value.(string)
		if !ok {
			return fmt.Errorf("unexpected type %T for field name", value)
		}
		m.SetName(v)
		return nil
	default:
		return fmt.Errorf("unknown Employee field %s", name)
	}
}

// AddedFields returns all numeric fields that were incremented or decremented during this mutation.
func (m *EmployeeMutation) AddedFields() []string {
	var fields []string
	return fields
}

// AddedField returns the numeric value that was incremented or decremented for a field.
func (m *EmployeeMutation) AddedField(name string) (runtime.Value, bool) {
	switch name {
	}
	return nil, false
}

// AddField adds the value for the given name. It returns an error if the field is not defined in the schema, or if the value type does not match.
func (m *EmployeeMutation) AddField(name string, value runtime.Value) error {
	switch name {
	default:
		return fmt.Errorf("unknown Employee addable field %s", name)
	}
}

// ClearedFields returns all fields that were cleared during this mutation.
func (m *EmployeeMutation) ClearedFields() []string {
	var fields []string
	for f := range m.clearedFields {
		fields = append(fields, f)
	}
	return fields
}

// FieldCleared reports if a field with the given name was cleared in this mutation.
func (m *EmployeeMutation) FieldCleared(name string) bool {
	_, ok := m.clearedFields[name]
	return ok
}

// ClearField clears the value of the field with the given name. It returns an error if the field is not defined in the schema or is not nullable.
func (m *EmployeeMutation) ClearField(name string) error {
	return fmt.Errorf("unknown Employee nullable field %s", name)
}

// ResetField resets all changes for the field with the given name. It returns an error if the field is not defined in the schema.
func (m *EmployeeMutation) ResetField(name string) error {
	switch name {
	case "name":
		m._name = nil
		delete(m.clearedFields, "name")
	default:
		return fmt.Errorf("unknown Employee field %s", name)
	}
	return nil
}

// AddedEdges returns all edge names that were set/added in this mutation.
func (m *EmployeeMutation) AddedEdges() []string {
	edges := make([]string, 0, 2)
	if m.subordinates != nil {
		edges = append(edges, "subordinates")
	}
	if m.manager != nil {
		edges = append(edges, "manager")
	}
	return edges
}

// AddedIDs returns all IDs that were added for the given edge name.
func (m *EmployeeMutation) AddedIDs(name string) []velox.Value {
	switch name {
	case "subordinates":
		ids := make([]velox.Value, 0, len(m.subordinates))
		for id := range m.subordinates {
			ids = append(ids, id)
		}
		return ids
	case "manager":
		ids := make([]velox.Value, 0, len(m.manager))
		for id := range m.manager {
			ids = append(ids, id)
		}
		return ids
	}
	return nil
}

// RemovedEdges returns all edge names that were removed in this mutation.
func (m *EmployeeMutation) RemovedEdges() []string {
	edges := make([]string, 0, 2)
	if m.removedSubordinates != nil {
		edges = append(edges, "subordinates")
	}
	if m.removedManager != nil {
		edges = append(edges, "manager")
	}
	return edges
}

// RemovedIDs returns all IDs that were removed for the given edge name.
func (m *EmployeeMutation) RemovedIDs(name string) []velox.Value {
	switch name {
	case "subordinates":
		ids := make([]velox.Value, 0, len(m.removedSubordinates))
		for id := range m.removedSubordinates {
			ids = append(ids, id)
		}
		return ids
	case "manager":
		ids := make([]velox.Value, 0, len(m.removedManager))
		for id := range m.removedManager {
			ids = append(ids, id)
		}
		return ids
	}
	return nil
}

// ClearedEdges returns all edge names that were cleared in this mutation.
func (m *EmployeeMutation) ClearedEdges() []string {
	edges := make([]string, 0, 2)
	if m.clearedSubordinates {
		edges = append(edges, "subordinates")
	}
	if m.clearedManager {
		edges = append(edges, "manager")
	}
	return edges
}

// EdgeCleared reports if the given edge was cleared in this mutation.
func (m *EmployeeMutation) EdgeCleared(name string) bool {
	switch name {
	case "subordinates":
		return m.clearedSubordinates
	case "manager":
		return m.clearedManager
	}
	return false
}

// ClearEdge clears the value of the edge with the given name.
func (m *EmployeeMutation) ClearEdge(name string) error {
	switch name {
	case "subordinates":
		m.ClearSubordinates()
		return nil
	case "manager":
		m.ClearManager()
		return nil
	}
	return fmt.Errorf("unknown Employee unique edge %s", name)
}

// ResetEdge resets all changes for the edge with the given name.
func (m *EmployeeMutation) ResetEdge(name string) error {
	switch name {
	case "subordinates":
		m.ResetSubordinates()
		return nil
	case "manager":
		m.ResetManager()
		return nil
	}
	return fmt.Errorf("unknown Employee edge %s", name)
}

// OldField returns the old value of a field from the database.
// An error is returned if the mutation operation is not UpdateOne,
// or the query to the database fails.
func (m *EmployeeMutation) OldField(ctx context.Context, name string) (velox.Value, error) {
	switch name {
	case "name":
		return m.OldName(ctx)
	}
	return nil, fmt.Errorf("unknown Employee field %s", name)
}

// OldName returns the old "name" field value, if exists.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *EmployeeMutation) OldName(ctx context.Context) (v string, err error) {
	old, loadErr := m.loadOld(ctx)
	if loadErr != nil {
		err = loadErr
		return
	}
	v = old.Name
	return
}
